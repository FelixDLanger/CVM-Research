# cvm-research — Python dependencies
#
# Most are pre-installed in Google Colab; the notebooks pip-install missing
# ones at the top. For local development with virtualenv:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   jupyter notebook

# Core data manipulation
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0           # parquet I/O

# Statistical analysis
scikit-learn>=1.3.0       # PCA, StandardScaler
statsmodels>=0.14.0       # OLS with cluster-robust SE

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0

# Data sources
simfin>=0.9.0             # SimFin Python wrapper (bulk CSV mode)
requests>=2.31.0          # World Bank API calls

# Jupyter (Colab pre-installs these; needed only for local Jupyter)
jupyter>=1.0.0
ipykernel>=6.25.0
